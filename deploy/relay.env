# DecentChat Relay Configuration
# Groups to host (comma-separated)
RELAY_GROUPS=lobby

# Listen port
RELAY_PORT=4433

# External IP to advertise (set for NAT/VPS deployments)
# RELAY_EXTERNAL_IP=203.0.113.5
